import type { PanelId } from "../shared/types";
const panels:Array<{id:PanelId;label:string;glyph:string;available:boolean}>=[{id:"weather",label:"Weather",glyph:"◉",available:true},{id:"tasks",label:"Tasks",glyph:"✓",available:false},{id:"news",label:"News",glyph:"N",available:false},{id:"business",label:"Business",glyph:"B",available:false}];
export function NavRail({selected,onSelect}:{selected:PanelId;onSelect:(id:PanelId)=>void}){return <aside className="nav-rail"><div className="rail-brand"><span>VIC</span><small>CONSOLE</small></div><nav aria-label="Console panels">{panels.map(panel=><button key={panel.id} className={selected===panel.id?"active":""} aria-current={selected===panel.id?"page":undefined} onClick={()=>onSelect(panel.id)}><i>{panel.glyph}</i><span>{panel.label}</span>{!panel.available&&<small>SOON</small>}</button>)}</nav><div className="rail-footer"><span className="secure-dot"/>LOCAL DATA</div></aside>}
