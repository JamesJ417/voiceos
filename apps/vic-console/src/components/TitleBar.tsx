import { getCurrentWindow } from "@tauri-apps/api/window";
export function TitleBar(){const window=getCurrentWindow();return <header className="titlebar" data-tauri-drag-region><div className="title-identity" data-tauri-drag-region><span className="vic-mark">V</span><strong>VIC Console</strong></div><div className="window-controls"><button aria-label="Minimize" onClick={()=>void window.minimize()}>—</button><button aria-label="Maximize or restore" onClick={()=>void window.toggleMaximize()}>□</button><button className="close" aria-label="Close" onClick={()=>void window.close()}>×</button></div></header>}
