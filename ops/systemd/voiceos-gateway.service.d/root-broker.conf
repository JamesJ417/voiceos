[Unit]
After=voiceos-root-broker.service
Wants=voiceos-root-broker.service

[Service]
Environment=VOICEOS_ROOT_BROKER_ENABLED=1
Environment=VOICEOS_ROOT_BROKER_SOCKET=/run/voiceos-root-broker/broker.sock
Environment=VOICEOS_ROOT_BROKER_KEY=/etc/voiceos/root-broker.key
