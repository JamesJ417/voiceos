# Agent Mail delivery configuration template
# Copy to a user-owned secret/config location; do not commit populated credentials.
VOICEOS_ARCHIVE_ROOT=/var/lib/voiceos/archive
AGENT_MAIL_ENDPOINT=
AGENT_MAIL_API_KEY=
AGENT_MAIL_SENDER=
AGENT_MAIL_TEST_RECIPIENT=
